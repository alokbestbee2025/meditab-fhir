{
  "version": 2,
  "buildCommand": "npm run generate",
  "framework": "nuxt",
  "builds": [
    {
      "src": "nuxt.config.ts",
      "use": "@nuxtjs/vercel-builder"
    }
  ],
  "routes": [
    {
      "src": "/api/.*",
      "dest": "/server/api/$1"
    }
  ]
}